# 媒体转录 Agent 依赖
# 本地 Windows 与服务器 Linux 通用（torch 用 CPU 版本，无 GPU 依赖）

# 飞书 Bot SDK
lark-oapi>=1.7.1

# 视频下载
yt-dlp>=2026.7.4

# 网页文章抽取（微信公众号、博客等）
trafilatura>=1.12.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ASR（FunASR + SenseVoiceSmall）
funasr>=1.3.14
modelscope>=1.38.1
faster-whisper>=1.2.1   # 备用 ASR 引擎（pipeline 支持切换）
transformers>=4.40.0     # FunASR 依赖

# PyTorch（CPU 版本）
# 注意：Linux/Docker 通过 --index-url 安装 CPU 版本
torch>=2.11.0
torchaudio>=2.11.0

# LLM 调用
openai>=2.0.0
tiktoken>=0.7.0          # OpenAI SDK token 计数依赖

# ffmpeg 兜底（系统未装 ffmpeg 时使用）
imageio-ffmpeg>=0.6.0

# HTTP 请求
requests>=2.31.0

# WebSocket（飞书长连接）
websockets>=12.0
